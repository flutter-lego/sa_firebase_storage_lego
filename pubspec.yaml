name: sa_firebase_storage_lego
description: "lego is all you need"
#publish_to: 'none'
repository: https://github.com/flutter-lego/sa_firebase_storage_lego

version: 1.0.1

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: #@add
  gap: #@add
  june_flow_util: #@add
  styled_widget: #@add
  modal_bottom_sheet: #@add
  keyboard_attachable: #@add
  flutter_zoom_drawer: #@add
  flutter_screenutil:
  bot_toast:
  firebase_core:
  firebase_storage: #@add
  path: #@add
  path_provider: #@add

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:
  screenutil_lego: #@add
  bot_toast_lego: #@add
  widget_binding_lego: #@add
  firebase_core_lego: #@add

flutter:
  assets:
    - assets/lego/sa_firebase_storage_lego/
  uses-material-design: true

topics:
  - lego